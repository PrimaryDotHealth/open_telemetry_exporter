# frozen_string_literal: true

require "bundler/gem_tasks"
require "rspec/core/rake_task"
require_relative "lib/open_telemetry_exporter/tasks.rake"

RSpec::Core::RakeTask.new(:spec)

task default: :spec 